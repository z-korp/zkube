# v1.3 Game Modes Implementation — Contract Status

> **Status:** Implemented and validated (contract side)
> **Date:** 2026-03-29
> **Scope:** Map + Endless modes, mutator hooks, mode-aware ranking, contract cleanup, runtime e2e without Torii
> **Source:** Implemented code + runtime verification

---

## Locked Decisions (Implemented)

| Decision | Answer |
|----------|--------|
| Endless difficulty driver | Total score with multiplier per tier (monotonic — only goes up) |
| Map L10 clear | Game ends (clean exit). No transition to endless. |
| Map ranking formula | `total_stars × 65536 + total_score` |
| Endless ranking formula | Pure `total_score` |
| Mutator mode eligibility | Tagged: map_only / endless_only / universal |
| Mutator frequency | One per run (both modes). Rolled at game start. |
| Map mutator pool | Gated to 3 per map (via GameSettings.allowed_mutators bitmask) |
| Endless/Daily mutator pool | Full pool (not gated) |
| Mode storage | RunData bit 101 (not in token settings_id) |
| Stars in Endless | No stars. Pure score. |

---

## RunData Layout (102 bits)

```
Bits 0-7:     current_level (u8)          — Map: 1-10, Endless: always 1
Bits 8-15:    level_score (u8)            — Map: per-level score, Endless: unused
Bits 16-23:   level_moves (u8)           — Map: moves this level, Endless: unused
Bits 24-31:   constraint_progress (u8)    — Map only, Endless: unused
Bits 32-39:   constraint_2_progress (u8)  — Map only, Endless: unused
Bits 40-47:   max_combo_run (u8)          — Both
Bits 48-79:   total_score (u32)           — Both
Bit 80:       zone_cleared (bool)         — Map: cleared L10, Endless: false
Bits 81-88:   current_difficulty (u8)     — Both: active difficulty tier (0-7)
Bits 89-92:   zone_id (u4)               — Reserved
Bits 93-100:  active_mutator_id (u8)      — Both: active mutator (0=none)
Bit 101:      mode (u1)                  — 0=Map, 1=Endless
```

Implemented changes vs v1.2:
- `endless_depth` (81-88) → `current_difficulty`
- `mutator_mask` (93-100) → `active_mutator_id`
- Added `mode` at bit 101
- Total layout now 102 bits

---

## Implementation Outcome

### Completed Workstreams

1. Types + packing migration
   - Added `GameMode`
   - Migrated RunData to 102-bit layout
   - Updated packing/unpacking and tests

2. Model updates
   - Added `PlayerBestRun`
   - Added mode/mutator-related fields where required
   - Updated daily challenge models for mode-aware behavior

3. Level generation split by mode
   - Map-mode progression and boss flow preserved/refined
   - Endless-mode generation and score-threshold difficulty escalation

4. Mode-aware creation/moves/game-over
   - `create(game_id, mode)` and run initialization path are mode-aware
   - Move processing branches by mode
   - Ranking and best-run persistence are mode-aware

5. Daily challenge mode support
   - Daily challenge includes mode and map context
   - Submission/settlement operate on mode-aware ranking semantics

6. Full contract dead-code cleanup
   - Removed v1.2 leftovers (cubes/bonuses/draft/shop/quest residues in contracts)
   - Updated constants/versioning and pruned obsolete paths

### Runtime Validation

- Slot flow validated without Torii:
  - mint token via game system
  - derive full packed token id as `game_id`
  - create run with mode
  - execute move successfully
- Added script:
  - `scripts/e2e_slot_without_torii.sh`

## Current Contract Flow (Operational)

1. Mint game token through minigame flow (`mint_game` / token mint path).
2. Reconstruct canonical `game_id` as full packed felt252.
3. Start run via `create(game_id, mode)`.
4. Execute actions via `move_system.move(...)`.
5. Persist mode-aware progression and ranking at run end.

## Verification Snapshot

- `scarb build`: pass
- `scarb test`: pass (`169 passed`)
- Runtime e2e (no Torii): pass

## Remaining Contract Risks / Follow-ups

| Risk | Severity | Mitigation |
|------|----------|------------|
| Mis-serialized token id at integration edges | Medium | Always pass full packed felt252 `game_id` to `create`/`move` |
| Legacy docs confusion | Medium | Continue trimming stale v1.2 references in contract docs |
| CASM size warning on FullTokenContract | Medium | Track upstream token-component footprint and optimize only if deployment constraints tighten |
