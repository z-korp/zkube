# v1.3 Game Modes Implementation Plan

> **Status:** Ready for implementation
> **Date:** 2026-03-29
> **Scope:** Add Endless mode, refactor Map mode, mutator hooks, mode-aware leaderboards
> **Source:** Metis analysis + user decisions

---

## Locked Decisions

| Decision | Answer |
|----------|--------|
| Endless difficulty driver | Total score with multiplier per tier (monotonic — only goes up) |
| Map L10 clear | Game ends (clean exit). No transition to endless. |
| Map ranking formula | `total_stars × 65536 + total_score` |
| Endless ranking formula | Pure `total_score` |
| Mutator mode eligibility | Tagged: map_only / endless_only / universal |
| Mutator frequency | One per run (both modes). Rolled at game start. |
| Map mutator pool | Gated to 3 per map (via GameSettings.allowed_mutators bitmask) |
| Endless/Daily mutator pool | Full pool (not gated) |
| Mode storage | RunData bit 101 (not in token settings_id) |
| Stars in Endless | No stars. Pure score. |

---

## RunData Layout (102 bits)

```
Bits 0-7:     current_level (u8)          — Map: 1-10, Endless: always 1
Bits 8-15:    level_score (u8)            — Map: per-level score, Endless: unused
Bits 16-23:   level_moves (u8)           — Map: moves this level, Endless: unused
Bits 24-31:   constraint_progress (u8)    — Map only, Endless: unused
Bits 32-39:   constraint_2_progress (u8)  — Map only, Endless: unused
Bits 40-47:   max_combo_run (u8)          — Both
Bits 48-79:   total_score (u32)           — Both
Bit 80:       zone_cleared (bool)         — Map: cleared L10, Endless: false
Bits 81-88:   current_difficulty (u8)     — Both: active difficulty tier (0-7)
Bits 89-92:   zone_id (u4)               — Reserved
Bits 93-100:  active_mutator_id (u8)      — Both: active mutator (0=none)
Bit 101:      mode (u1)                  — 0=Map, 1=Endless
```

Changes from current:
- `endless_depth` (81-88) → `current_difficulty` (repurposed)
- `mutator_mask` (93-100) → `active_mutator_id` (renamed)
- Added `mode` at bit 101
- Total: 102 bits (was 101)

---

## Commit Plan (11 commits, TDD)

### Commit 1: `feat(types): add GameMode enum and update RunData packing`
**Files:** `types/mode.cairo` (NEW), `helpers/packing.cairo`, `lib.cairo`
**Changes:**
- GameMode enum: Map(0), Endless(1)
- Rename endless_depth → current_difficulty in RunData
- Rename mutator_mask → active_mutator_id
- Add mode bit at position 101
- Update pack/unpack
**Tests:** Roundtrip pack/unpack for all field combos, mode=0 and mode=1
**Verify:** `scarb test`

### Commit 2: `feat(models): add PlayerBestRun, add mutator_id to GameLevel`
**Files:** `models/player.cairo`, `models/game.cairo`, `lib.cairo`
**Changes:**
- PlayerBestRun model: (player, settings_id, mode) → best_score, best_stars, best_level, map_cleared, best_game_id
- GameLevel gains mutator_id field
**Tests:** Model instantiation
**Verify:** `scarb build`

### Commit 3: `feat(models): update DailyChallenge for mode support`
**Files:** `models/daily.cairo`
**Changes:**
- Add mode (u8) and map_settings_id (u32) to DailyChallenge
**Tests:** Model instantiation
**Verify:** `scarb build`

### Commit 4: `feat(config): add mutator and endless difficulty fields to GameSettings`
**Files:** `models/config.cairo`, `systems/config.cairo`
**Changes:**
- GameSettings gains: allowed_mutators (u32), endless_difficulty_thresholds (felt252), endless_score_multipliers (felt252)
- Default settings presets updated with mutator masks and thresholds
- Admin function: set_mutator_config()
**Tests:** Settings creation, threshold packing
**Verify:** `scarb test`

### Commit 5: `feat(types): add mutator trait and no-op implementation`
**Files:** `types/mutator.cairo` (NEW), `lib.cairo`
**Changes:**
- MutatorTrait: modify_level_config, modify_score, modify_block_weights hooks
- MutatorContext struct
- Default no-op implementation
- roll_mutator() function with pool filtering
- MUTATOR_NONE = 0 constant
**Tests:** No-op tests, roll_mutator with various masks
**Verify:** `scarb test`

### Commit 6: `feat(level): dual-path level generation for Map and Endless`
**Files:** `helpers/level.cairo`, `systems/level.cairo`
**Changes:**
- generate() dispatches to generate_map_level() or generate_endless_level()
- generate_map_level(): extract current zone logic (L1-10, constraints, boss at 10)
- generate_endless_level(): VeryEasy, max moves, no constraints
- get_endless_difficulty(total_score, thresholds) → Difficulty
- initialize_endless_level() on level system
- update_endless_difficulty() on level system
- Mutator rolling in generate_map_level()
- L10 clear = game ends (remove endless transition)
**Tests:** Map gen at L1/5/10, endless gen, difficulty threshold crossing, mutator rolling
**Verify:** `scarb test`

### Commit 7: `feat(game): mode-aware game creation`
**Files:** `systems/game.cairo`, `helpers/game_helpers.cairo`
**Changes:**
- create(game_id, mode) and create_run(game_id, mode) gain mode parameter
- Branch in create_game(): Map init vs Endless init
- Endless: init perpetual level, roll mutator from full pool
- Map: init level 1, roll mutator from map's gated pool
- create_challenge_game(game_id, challenge_id): daily flow, bypasses entitlement
**Tests:** Create map/endless/challenge games, verify RunData mode bit
**Verify:** `scarb test`

### Commit 8: `feat(moves): mode-aware post-move logic`
**Files:** `systems/moves.cairo`
**Changes:**
- After scoring, branch on mode:
  - Map: level completion/failure checks (existing)
  - Endless: check difficulty threshold → update difficulty, apply multiplier, check grid full
- maybe_update_difficulty() helper
- Score multiplier application for endless
**Tests:** Map level complete/fail, endless grid full, difficulty threshold crossing
**Verify:** `scarb test`

### Commit 9: `feat(game_over): mode-aware ranking and PlayerBestRun`
**Files:** `helpers/game_over.cairo`
**Changes:**
- Replace PlayerMeta.best_level with PlayerBestRun upsert
- compute_ranking_value(): Map = stars×65536+score, Endless = score
- RunEnded event gains mode field
- Daily detection via GameChallenge model (not settings_id range)
**Tests:** Map/Endless game over, best run update logic, daily auto-submit
**Verify:** `scarb test`

### Commit 10: `feat(daily): mode-aware daily challenges`
**Files:** `systems/daily_challenge.cairo`
**Changes:**
- create_daily_challenge(mode, map_settings_id, prize_amount)
- Remove settings_id 100-109 range, ranking_metric parameter
- Auto-derive ranking from mode
- Settlement uses mode-specific formula
**Tests:** Create daily for each mode, settlement ranking
**Verify:** `scarb test`

### Commit 11: `refactor(cleanup): remove deprecated code`
**Files:** Multiple
**Changes:**
- Remove is_daily_challenge_settings()
- Remove DAILY_CHALLENGE_SETTINGS_ID range constants
- Remove zone→endless transition in advance_level()
- Clean dead code paths
**Tests:** Full integration test: create+play+game_over for both modes
**Verify:** `scarb test && scarb build`

---

## Dependency Graph

```
Commit 1 (types/packing) ──┬── Commit 2 (models) ──┬── Commit 6 (level gen) ── Commit 8 (moves)
                            │                        │
                            ├── Commit 3 (daily model)├── Commit 7 (game create) ── Commit 9 (game over)
                            │                        │
                            ├── Commit 4 (config)    └── Commit 10 (daily system)
                            │                                        │
                            └── Commit 5 (mutator type)              └── Commit 11 (cleanup)

Parallelizable: [2, 3, 4, 5] after 1
Parallelizable: [6, 7] after [2, 4, 5]
Sequential: 8 after 6, 9 after [7, 8], 10 after [3, 9], 11 after all
```

---

## Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Score multiplier creates inflation | Medium | Multiplier capped at 3.0×, thresholds tunable per map |
| Mutator balance unknown | Low | No-op implementation, effects added incrementally |
| 6 leaderboards with small alpha base | Medium | Consider aggregating to 2 global boards if needed |
| RunData layout change | Low | Deploy to fresh slot/namespace, no in-flight games |
| Daily without special settings_id | Medium | Use GameChallenge model for detection |
